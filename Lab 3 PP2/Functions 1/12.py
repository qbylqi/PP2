def histogram(lst):
    for num in lst:
        print('*' * num)

# Пример использования
histogram([4, 9, 7])
#Объяснение: функция строит гистограмму, выводя количество звёздочек для каждого числа.